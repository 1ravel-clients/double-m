<?xml version='1.0' encoding='utf-8'?>
<odoo>

<record id="view_task_form_inherit" model="ir.ui.view">
    <field name="name">view_task_form_inherit</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_form2"/>
    <field name="arch" type="xml">
        <!-- Hide customer field -->
        <xpath expr="//field[@name='partner_id']" position="attributes">
            <attribute name="invisible">1</attribute>
        </xpath>
        <xpath expr="//field[@name='partner_id']" position="after">
            <field name="contact_id"/>
            <field name="contact_phone"/>
            <field name="current_user_id" invisible="1"/>
            <field name="current_user_country" invisible="1"/>
        </xpath>
        <xpath expr="//notebook" position="inside">
           <page name="contact" string="Contacts &amp; Addresses">
                <field name="contact_info_ids">
<!--                    <list editable="bottom" create="false">-->
<!--                        <field name="name"/>-->
<!--                        <field name="current_user_id" invisible="1"/>-->
<!--                        <field name="current_user_country" invisible="1"/>-->
<!--                        <field name="phone" />-->
<!--                    </list>-->

                    <kanban color="color">
                        <field name="color"/>
                        <field name="avatar_128"/>
                        <field name="name"/>
                        <field name="type"/>
                        <field name="email"/>
                        <field name="parent_id"/>
                        <field name="is_company"/>
                        <field name="function"/>
                        <field name="phone"/>
                        <field name="street"/>
                        <field name="street2"/>
                        <field name="zip"/>
                        <field name="city"/>
                        <field name="country_id"/>
                        <field name="state_id"/>
                        <field name="image_128"/>
                        <field name="lang"/>
                        <!-- fields in form x2many view to diminish requests -->
                        <field name="comment"/>
                        <field name="display_name"/>
                        <templates>
                            <t t-name="card" class="flex-row">
                                <aside class="o_kanban_aside_full">
                                    <field name="avatar_128" class="o_kanban_image_fill w-100" widget="image" options="{'img_class': 'object-fit-cover'}" alt="Contact image" invisible="type != 'contact'"/>
                                    <field name="avatar_128" class="o_kanban_image_fill w-100" widget="image" options="{'img_class': 'object-fit-contain p-2'}" alt="Contact image" invisible="type == 'contact'"/>
                                </aside>
                                <main class="ps-2 ps-md-0">
                                    <field name="name" invisible="not name" class="fw-bold"/>
                                    <field name="type" invisible="name" class="fw-bold"/>
                                    <div t-if="record.email.raw_value" class="text-truncate">
                                        <i class="fa fa-fw me-1 fa-envelope text-primary" title="Email"/>
                                        <field name="email"/>
                                    </div>
                                    <div t-if="record.phone.raw_value">
                                        <i class="fa fa-fw me-1 fa-phone text-primary" title="Phone"/>
                                        <field name="phone"/>
                                    </div>
                                    <div invisible="not function or is_company">
                                        <i class="fa fa-fw me-1 fa-suitcase text-primary" title="Position"/>
                                        <field name="function"/>
                                    </div>
                                    <div t-if="record.type.raw_value != 'contact' and (record.city.raw_value or record.country_id.raw_value)">
                                        <i class="fa fa-fw me-1 fa-map-marker text-primary" title="Location"/>
                                        <field name="city"/>
                                        <span t-if="record.city.raw_value and record.country_id.raw_value">, </span>
                                        <field name="country_id"/>
                                    </div>
                                </main>
                            </t>
                        </templates>
                    </kanban>



                    <form string="Contact / Address">
                        <sheet>
                            <field name="type" required="1" widget="radio" options="{'horizontal': true}"/>
                            <field name="parent_id" invisible="1"/>
                            <hr/>
                            <group col="12">
                                <group colspan="5">
                                    <field name="name" string="Contact Name" required="type == 'contact'"/>
                                    <field name="function" placeholder="e.g. Sales Director" invisible="type != 'contact'"/>
                                    <label for="street" string="Address" invisible="type == 'contact'"/>
                                    <div invisible="type == 'contact'">
                                        <div class="o_address_format" name="div_address">
                                            <field name="street" placeholder="Street..." class="o_address_street"/>
                                            <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                                            <field name="city" placeholder="City" class="o_address_city"/>
                                            <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                                            <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                                            <field name="country_id" placeholder="Country" class="o_address_country" options="{&quot;no_open&quot;: True, &quot;no_create&quot;: True}"/>
                                        </div>
                                    </div>
                                    <field name="comment" placeholder="Internal notes..."/>
                                </group>
                                <group colspan="5">
                                    <field name="current_user_id" invisible="1"/>
                                    <field name="current_user_country" invisible="1"/>
                                    <field name="email" widget="email"/>
                                    <field name="phone" invisible="current_user_country == 'vietnam'"/>
                                    <field name="contact_phone_gcalls" widget="outbound_call" invisible="current_user_country == 'indo'"/>
                                    <field name="company_id" invisible="1"/>
                                </group>
                                <group colspan="1">
                                    <field name="image_1920" widget="image" class="oe_avatar" nolabel="1" options="{'image_preview': 'image_128'}"/>
                                </group>
                            </group>
                            <group>
                                <button name="action_make_biz_call" type="object" string="Make Call" class="oe_highlight" invisible="current_user_country == 'vietnam'"/>
                            </group>
                            <field name="lang" invisible="1"/>
                            <field name="user_id" invisible="1"/>
                            <field name="image_128" invisible="1"/>
                        </sheet>
                    </form>
                </field>
           </page>
        </xpath>
    </field>
</record>

</odoo>
