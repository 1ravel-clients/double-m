<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Remove native "Create Project" from the SO gear menu.
         We use per-line project creation instead. -->
    <record id="sale_project.model_sale_order_action_create_project" model="ir.actions.server">
        <field name="binding_model_id" eval="False"/>
    </record>
</odoo>
